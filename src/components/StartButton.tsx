import React from 'react';
import { StyledStartButton } from './styles/StyledStartButton';

const StartGame = ({ callback = null }) => (
    <StyledStartButton>Start game</StyledStartButton>
)
export default StartGame;